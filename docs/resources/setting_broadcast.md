---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "unifi_setting_broadcast Resource - terraform-provider-unifi"
subcategory: ""
description: |-
  The unifi_setting_broadcast resource manages the media Broadcast settings for a UniFi site, controlling the sounds played before and after a broadcast.
---

# unifi_setting_broadcast (Resource)

The `unifi_setting_broadcast` resource manages the media Broadcast settings for a UniFi site, controlling the sounds played before and after a broadcast.

## Example Usage

```terraform
resource "unifi_setting_broadcast" "example" {
  # Play a sound before a broadcast starts
  sound_before_enabled  = true
  sound_before_resource = "chime"
  sound_before_type     = "sample" # valid values: "sample" or "media"

  # Play a sound after a broadcast ends
  sound_after_enabled  = true
  sound_after_resource = "chime"
  sound_after_type     = "sample" # valid values: "sample" or "media"

  # Specify the site (optional, defaults to site configured in provider, otherwise "default")
  # site = "default"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `site` (String) The name of the UniFi site where this resource should be applied. If not specified, the default site will be used.
- `sound_after_enabled` (Boolean) Whether a sound is played after a broadcast.
- `sound_after_resource` (String) The resource identifier of the sound played after a broadcast.
- `sound_after_type` (String) The type of the sound played after a broadcast. Valid values are `sample` and `media`.
- `sound_before_enabled` (Boolean) Whether a sound is played before a broadcast.
- `sound_before_resource` (String) The resource identifier of the sound played before a broadcast.
- `sound_before_type` (String) The type of the sound played before a broadcast. Valid values are `sample` and `media`.

### Read-Only

- `id` (String) The unique identifier of this resource.
